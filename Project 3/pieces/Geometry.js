class Geometry extends CGFobject {

	constructor(scene) {
        super(scene);
    }

    /**
     * 
     * @param {Duration of the animation} span 
     */
    getAnimation(span){
        return console.error("getAnimation is not declared");
        
    }
    
    display(){
    }
}